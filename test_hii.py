from hii import hello
def test_hello():
    assert hello("wings")=="hello, wings"
    assert hello()=="hello, world"
   # assert hello("info")=="hello , info"
# actually this code has been might bugs to resolve the next code 